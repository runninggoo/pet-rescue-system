# 毕业设计论文素材保存文档

> 本文档用于保存开发过程中有利于撰写毕业论文和开发周记的信息，包括测试数据、系统截图、代码亮点等。
>
> **版本**：v1.2.4
> **更新日期**：2026-04-27

---

## 一、项目基本信息（论文摘要用）

### 1.1 项目名称
**基于Spring Boot的宠物救助收养系统**

### 1.2 项目背景
随着社会经济与宠物饲养文化的发展，流浪宠物数量激增，已成为不容忽视的城市治理与公共卫生问题。当前宠物救助收养领域面临三大核心痛点：
1. **信息碎片化**导致匹配效率低下
2. **流程非标准化**引发运营与信任危机
3. **多主体协同缺失**造成资源浪费

### 1.3 核心技术栈

| 类别 | 技术 |
|------|------|
| 后端框架 | Spring Boot 2.7.0 + Spring Security |
| 持久层 | MyBatis Plus 3.5.3.1 |
| 数据库 | MySQL 8.0+ |
| 前端框架 | Vue.js 2.6.14 + Element UI 2.15.13 |
| 认证方式 | JWT (JSON Web Token) + Refresh Token 双Token架构 |
| 构建工具 | Vite（前端开发服务器） |

### 1.4 系统功能模块

| 功能 | 说明 |
|------|------|
| 用户认证与权限管理 | 5种角色：admin/institution_admin/volunteer/adopter/pet_hospital |
| 宠物信息管理 | CRUD + 多条件搜索 + 品种两级联动分类 |
| 领养申请流程 | 提交 → 审核 → 签约 → 回访，含**多申请人对比**功能 |
| 救助站智能推荐 | 基于Haversine距离算法 + 多因素权重评分（核心亮点） |
| 数据统计看板 | 9个ECharts图表 + 简化版统计页 |
| 健康档案管理 | 疫苗/体检/治疗时间线记录 |
| 协同任务管理 | 志愿者任务接单 + 管理员发布 |
| **志愿者成长体系** | 积分/签到/等级（6级）/排行榜 |
| **宠物收藏功能** | 收藏/取消收藏/我的收藏 |
| **帮助中心页面** | 新手引导、使用指南、领养/志愿者流程说明 |
| **宠物医院入口** | 合作宠物医院展示页面 |
| **系统入口页** | 统一入口导航 |

### 1.5 创新点

1. **全链路数字化** — 覆盖救助发现到领养后跟踪回访的完整业务流程
2. **多角色协同** — 支持5类用户角色的精细权限控制与工作协同
3. **救助站智能推荐** — 基于Haversine地理距离和权重评分的救助站智能匹配算法
4. **个性化宠物推荐** — 基于用户行为记录（浏览/收藏/申请）计算偏好向量，为用户推荐感兴趣的宠物
5. **志愿者成长激励** — 6级成长体系 + 每日签到 + 积分历史追踪，游戏化激励志愿者参与
6. **个性化收藏** — 用户可收藏心仪宠物，便于后续申请和关注
7. **数据驱动决策** — 集成统计看板支持管理决策

---

## 二、测试数据

### 2.1 测试账号

| 角色 | 手机号 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | 13800000000 | 123456 | 系统管理员，拥有所有权限 |
| 领养人 | 13900000001 | 123456 | 普通用户，可申请领养 |
| 机构管理员 | 13700000001 | 123456 | 救助机构管理者 |
| 志愿者 | 13600000001 | 123456 | 可参与救助活动、成长中心 |
| 宠物医院 | 13500000001 | 123456 | 可管理宠物健康档案 |

### 2.2 领养申请测试数据

| ID | 申请人 | 宠物 | 状态 | 申请时间 |
|----|--------|------|------|----------|
| 4 | 赵六 | 花花（猫咪） | 0待审核 | 2026-03-31 |
| 5 | 李四 | 小黑（拉布拉多） | 0待审核 | 2026-03-30 |
| 6 | 王五 | 小橘 | 1已通过 | 2026-03-27 |
| 7 | 赵六 | 旺财（金毛） | 1已通过 | 2026-03-25 |
| 8 | 孙七 | 黑猫警长 | 2已拒绝 | 2026-03-22 |
| 9 | 周八 | 毛毛 | 3待补充 | 2026-03-29 |
| 10 | 吴九 | 小白（英短） | 4待签署 | 2026-03-24 |

> **状态说明**：0=待审核, 1=已通过, 2=已拒绝, 3=待补充材料, 4=待签署协议, 5=已完成

### 2.3 救助站测试数据

| 名称 | 区域 | 纬度 | 经度 | 最大容量 | 医疗等级 |
|------|------|------|------|----------|----------|
| 上海小动物保护中心 | 浦东新区 | 31.2304 | 121.4737 | 100 | A |
| 浦东爱心救助站 | 浦东新区 | 31.2450 | 121.5200 | 50 | B |
| 徐汇流浪动物之家 | 徐汇区 | 31.1880 | 121.4380 | 80 | A |
| 闵行宠物救助中心 | 闵行区 | 31.1130 | 121.3750 | 60 | B |
| 静安区动物保护协会 | 静安区 | 31.2290 | 121.4650 | 40 | A |

### 2.4 智能推荐算法测试用例

**测试位置**：上海陆家嘴（纬度：31.2397，经度：121.4998）

| 范围 | 预期返回数量 | 备注 |
|------|--------------|------|
| 3公里 | 1-2个 | 最近救助站 |
| 5公里 | 2-3个 | 覆盖浦东区 |
| 10公里 | 4-5个 | 覆盖浦东+徐汇+静安 |

**算法参数**：距离权重40% + 容量权重30% + 医疗等级权重30%

---

## 三、数据库设计

### 3.1 核心数据表（v1.2.4）

| 表名 | 说明 | 主键 | 外键 |
|------|------|------|------|
| user | 用户表 | id | institution_id |
| pet | 宠物表 | id | institution_id, adopter_id |
| pet_category | 品种分类表（邻接表） | id | parent_id（自关联） |
| shelter | 救助站表 | id | institution_id |
| adoption_application | 领养申请表 | id | applicant_id, pet_id |
| follow_up_record | 回访记录表 | id | application_id, follower_id |
| volunteer_task | 志愿者任务表 | id | publisher_id, volunteer_id |
| health_record | 健康档案表 | id | pet_id, institution_id |
| volunteer_profile | 志愿者档案表 | id | user_id |
| point_history | 积分历史表 | id | user_id |
| level_rule | 等级规则表 | id | — |
| pet_favorite | 宠物收藏表 | id | user_id, pet_id |
| refresh_token | 刷新令牌表 | id | user_id |
| operation_log | 操作日志表 | id | user_id |

### 3.2 E-R图关系

```
用户 ─────┬───── 机构 ───── 宠物
         │                  │
         ├───── 志愿者      │
         │                  │
         ├───── 医院 ───────┴───── 领养申请 ──── 回访记录
         │                         │
         ├───── 志愿者任务          │
         │                  （已领养的宠物）
         └───── 积分历史 ── 等级规则
         
用户 ── 宠物收藏（多对多）
用户 ── 刷新令牌（1对1）
```

### 3.3 重要字段说明

**pet_category（邻接表，两级联动）**：
- `parent_id IS NULL` → 根节点（一级分类，如"猫"、"狗"）
- `parent_id IS NOT NULL` → 子节点（品种，如"中华田园猫"）

**adoption_application（6种状态）**：
- 0=待审核, 1=已通过, 2=已拒绝, 3=待补充, 4=待签署, 5=已完成

**refresh_token（双Token机制）**：
- 存储 Refresh Token，与用户一一对应
- 包含 user_agent、ip_address 等设备追踪信息
- 支持主动注销（删除记录即失效）

---

## 四、关键代码片段

### 4.1 Haversine距离计算算法

```java
/**
 * 使用Haversine公式计算两点之间的距离（单位：公里）
 * @param lat1 用户纬度
 * @param lon1 用户经度
 * @param lat2 救助站纬度
 * @param lon2 救助站经度
 */
public static double calculateDistance(double lat1, double lon1, double lat2, double lon2) {
    final double R = 6371; // 地球半径（公里）
    double latDistance = Math.toRadians(lat2 - lat1);
    double lonDistance = Math.toRadians(lon2 - lon1);
    double a = Math.sin(latDistance / 2) * Math.sin(latDistance / 2)
            + Math.cos(Math.toRadians(lat1)) * Math.cos(Math.toRadians(lat2))
            * Math.sin(lonDistance / 2) * Math.sin(lonDistance / 2);
    double c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    return R * c;
}
```

### 4.2 救助站智能推荐权重评分算法

```java
/**
 * 计算救助站综合评分
 * @param distance 距离（公里）
 * @param maxDistance 最大搜索距离
 * @param remainingCapacity 剩余容量
 * @param maxCapacity 最大容量
 * @param medicalLevel 医疗等级（A/B/C）
 * @return 综合评分（0-100）
 */
public int calculateScore(double distance, double maxDistance,
                          int remainingCapacity, int maxCapacity, String medicalLevel) {
    // 距离得分：距离越近得分越高（权重40%）
    double distanceScore = 40 * (1 - distance / maxDistance);

    // 容量得分：剩余容量比例越高得分越高（权重30%）
    double capacityRatio = (double) remainingCapacity / maxCapacity;
    double capacityScore = 30 * capacityRatio;

    // 医疗等级得分（权重30%）
    double medicalScore = "A".equals(medicalLevel) ? 30 :
                         "B".equals(medicalLevel) ? 20 : 10;

    return (int) (distanceScore + capacityScore + medicalScore);
}
```

### 4.2 个性化推荐算法

```java
/**
 * 基于用户行为偏好的个性化推荐算法
 * Score = α × 类别匹配度 + β × 紧急程度 + γ × 新鲜度
 * α=0.5, β=0.3, γ=0.2
 *
 * 步骤：
 * 1. 从 user_pet_behavior 表聚合用户行为（apply=5分/favorite=3分/view=1分）
 * 2. 映射 petId → categoryType，计算归一化偏好向量（取 Top 3 分类）
 * 3. 候选宠物 = 待领养(status=0) 且 未交互
 * 4. 对候选宠物加权打分，取 Top N 返回
 */
public List<Pet> getPersonalizedRecommendations(Long userId, int limit) {
    Map<String, Double> categoryPreference = computeCategoryPreference(userId);
    if (categoryPreference.isEmpty()) {
        return getHotPets(limit); // 无偏好时返回最新宠物
    }
    Set<Long> interactedPetIds = getInteractedPetIds(userId);
    // 过滤、评分、排序后返回
}
```

### 4.4 JWT双Token认证机制

```java
// 登录时生成双Token
String accessToken = jwtTokenUtil.generateToken(userDetails);
String refreshToken = refreshTokenService.createRefreshToken(user.getId());

// Token刷新接口
public ResponseResult refresh(String refreshToken) {
    RefreshToken token = refreshTokenService.verifyRefreshToken(refreshToken);
    String newAccessToken = jwtTokenUtil.generateToken(userDetailsService.loadUserByUsername(...));
    return ResponseResult.success(Map.of("token", newAccessToken));
}
```

### 4.5 志愿者成长等级体系

```java
// 6级成长体系
public static final String[] LEVEL_NAMES = {
    "新手上路", "小小志愿者", "爱心使者", "救助达人", "金牌志愿者", "公益之星"
};
// 等级积分阈值：0, 100, 300, 600, 1000, 2000

// 积分获得规则
public static int getPointsForAction(String action) {
    return switch (action) {
        case "daily_sign" -> 5;          // 每日签到
        case "continuous_7_sign" -> 20;    // 连续7天签到额外奖励
        case "task_complete" -> 10;        // 完成任务
        default -> 0;
    };
}
```

---

## 五、系统架构图

```
┌────────────────────────────────────────────────────────────────────┐
│                      前端层 (Vue.js 2.6.14 + Element UI)               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────┐│
│  │ 登录页面 │ │宠物列表  │ │领养申请  │ │数据统计  │ │个人中心 ││
│  │ 注册页面  │ │宠物详情  │ │申请管理  │ │用户管理  │ │成长中心 ││
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ │我的收藏 ││
│  ┌────────────────────────────────────────────┐     └─────────┘│
│  │ 救助站智能推荐页面（核心亮点：Haversine算法）  │              │
│  └────────────────────────────────────────────┘              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │健康档案  │ │志愿者任务│ │宠物医院  │ │帮助中心  │        │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘        │
└────────────────────────────────────────────────────────────────────┘
                               │
                               ▼ HTTP/REST（Axios）
┌────────────────────────────────────────────────────────────────────┐
│               API网关层 (Spring Security + JWT 双Token)                 │
│  context-path: /api                                                 │
└────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌────────────────────────────────────────────────────────────────────┐
│                    业务逻辑层 (Spring Boot 2.7.0)                      │
│  用户服务  宠物服务  领养服务  推荐服务  统计服务  成长服务  收藏服务│
└────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌────────────────────────────────────────────────────────────────────┐
│                    数据访问层 (MyBatis Plus 3.5.3.1)                  │
│                    Mapper 接口层（自动SQL生成）                         │
└────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌────────────────────────────────────────────────────────────────────┐
│                      数据层 (MySQL 8.0)                               │
│  用户表  宠物表  申请表  机构表  救助站表  健康档案表  志愿者任务表  │
│  积分历史表  收藏表  刷新令牌表  操作日志表  品种分类表  回访记录表 │
└────────────────────────────────────────────────────────────────────┘
```

---

## 六、论文各章节规划

### 第1章 绪论
- [ ] 1.1 研究背景和意义
- [ ] 1.2 国内外研究概况
- [ ] 1.3 本文的主要工作和章节安排

### 第2章 相关技术和理论
- [ ] 2.1 Spring Boot框架介绍
- [ ] 2.2 Vue.js前端框架
- [ ] 2.3 MyBatis Plus持久层框架
- [ ] 2.4 JWT认证机制 + Refresh Token双Token架构
- [ ] 2.5 Haversine距离算法

### 第3章 系统需求分析与设计
- [ ] 3.1 系统需求分析
- [ ] 3.2 系统功能设计
- [ ] 3.3 数据库设计（E-R图、数据表）
- [ ] 3.4 救助站智能推荐算法设计

### 第4章 系统实现
- [ ] 4.1 开发环境配置
- [ ] 4.2 用户认证模块实现（含JWT刷新机制）
- [ ] 4.3 宠物管理模块实现（含收藏功能、个性化推荐模块）
- [ ] 4.4 领养申请模块实现（含多申请人对比、回访记录）
- [ ] 4.5 救助站智能推荐模块实现（Haversine + 权重评分）
- [ ] 4.6 健康档案与志愿者任务模块实现
- [ ] 4.7 志愿者成长体系实现（积分/签到/排行榜）
- [ ] 4.8 数据统计模块实现（ECharts + 简化版）
- [ ] 4.9 用户管理模块实现（隐私脱敏）

### 第5章 系统测试
- [ ] 5.1 测试环境
- [ ] 5.2 功能测试
- [ ] 5.3 性能测试
- [ ] 5.4 测试结果分析

### 第6章 总结与展望
- [ ] 6.1 总结
- [ ] 6.2 展望

---

## 七、答辩可能问题及回答

### Q1: 为什么选择Spring Boot框架？
**A**: Spring Boot简化了Spring应用的初始搭建和开发过程，提供了自动配置功能，能够快速构建独立运行的生产级Spring应用程序。

### Q2: 如何保证系统安全性？
**A**: 本系统采用JWT（JSON Web Token）进行用户认证，结合Spring Security实现基于角色的权限控制。同时引入Refresh Token双Token机制，Access Token短期有效（30分钟），Refresh Token存数据库（7天），支持主动注销和安全刷新。

### Q3: 智能推荐算法如何实现？
**A**: 采用Haversine公式计算用户与救助站之间的地理距离，结合救助站容量和医疗等级进行加权评分（距离40% + 容量30% + 医疗等级30%），返回综合评分最高的救助站列表。

### Q4: 数据库设计遵循什么原则？
**A**: 遵循数据库设计三范式，确保数据完整性，避免数据冗余。核心表之间通过外键关联，保证数据一致性。使用MyBatis Plus的逻辑删除（@TableLogic）实现软删除。

### Q5: 系统有什么创新点？
**A**: 1. 全链路数字化管理；2. 多角色协同工作；3. 基于地理位置的救助站智能推荐；4. 完整的领养流程追踪（含回访记录）；5. 志愿者成长激励体系；6. 个性化宠物收藏。

### Q6: JWT双Token机制相比单Token有什么优势？
**A**: Access Token频繁传输存在泄露风险，双Token机制将短期认证令牌与长期刷新令牌分离，Access Token泄露后攻击窗口仅30分钟；Refresh Token存服务端数据库，支持主动注销任意设备会话；结合User-Agent和IP记录，可追踪令牌创建时的设备信息。

### Q7: 志愿者成长体系如何设计？
**A**: 6级成长体系（新手上路 → 公益之星），积分来源包括每日签到（+5分/天）、连续7天签到额外奖励（+20分）、完成任务（+10分/任务）。系统自动根据累计积分计算当前等级，排行榜展示TOP10志愿者。

### Q8: 宠物收藏功能如何实现？
**A**: 用户可对心仪宠物点击爱心图标收藏，数据存储在 pet_favorite 表（user_id + pet_id，唯一约束防止重复收藏）。采用 MySQL `INSERT ... ON DUPLICATE KEY UPDATE` 实现收藏/取消的原子操作。个人中心「我的收藏」Tab展示收藏列表。

### Q9: 如何解决品种分类的多级联动问题？
**A**: 采用邻接表模型（Adjacency List）存储品种分类，`parent_id` 为空表示一级分类（如"猫"、"狗"），非空表示二级分类（如"中华田园猫"）。通过 MyBatis Plus 一次性查出所有子品种按 parentId 分组，避免 N+1 查询问题。

### Q10: 个性化推荐算法如何实现？
**A**: 基于用户在平台上的行为记录（浏览/收藏/申请），从 `user_pet_behavior` 表聚合加权得分，映射宠物ID到分类类型，计算用户偏好向量（Top 3）。候选宠物为待领养且未交互的宠物，通过加权评分公式 `Score = 0.5×类别匹配度 + 0.3×紧急程度 + 0.2×新鲜度` 排序后返回推荐结果。无偏好用户则返回最新宠物。

### Q12: 遇到的最大技术困难是什么？
**A**: MyBatis Plus 逻辑删除与全局配置冲突导致分页 COUNT=0 的问题。根因是数据库 `deleted` 列为 NULL 而非 0，与全局配置和 @TableLogic 注解三者冲突。修复方案：清理 NULL 值 + 移除全局配置 + 改用手动 COUNT。

---

## 八、前端页面清单（v1.2.4 完整版）

| # | 页面文件 | 访问路径 | 功能说明 |
|---|----------|----------|----------|
| 1 | login.html | `/login.html` | 用户登录（JWT认证 + 角色切换） |
| 2 | register.html | `/register.html` | 用户注册（手机号+密码+角色） |
| 3 | index.html | `/index.html` | 系统入口页（导航 + 功能卡片 + 领养流程） |
| 4 | pet-list.html | `/pet-list.html` | 宠物列表浏览 + 筛选 + 分页 + 管理增删 |
| 5 | pet-detail.html | `/pet-detail.html` | 宠物详情 + 领养申请 + **收藏** + 健康档案入口 |
| 6 | adoption-list.html | `/adoption-list.html` | 管理员审核领养申请 + **多申请人对比** + **回访记录** |
| 7 | shelter-recommendation.html | `/shelter-recommendation.html` | 救助站智能推荐（亮点功能） |
| 8 | stats.html | `/stats.html` | 数据统计看板（9个ECharts图表） |
| 9 | simple-stats.html | `/simple-stats.html` | 简化版统计页 |
| 10 | user-management.html | `/user-management.html` | 用户管理 + 隐私脱敏展示 |
| 11 | health-record.html | `/health-record.html` | 健康档案管理（时间线） |
| 12 | volunteer-task.html | `/volunteer-task.html` | 志愿者任务接单 + 成长积分 |
| 13 | profile.html | `/profile.html` | 个人中心 + **成长中心** + **我的收藏** |
| 14 | pet-hospital.html | `/pet-hospital.html` | 宠物医院入口 |
| 15 | help.html | `/help.html` | 帮助中心/使用指南/FAQ |
| 16 | simple-stats.html | `/simple-stats.html` | 简化统计页 |

> **Vite 开发端口**：5173（`http://localhost:5173/{page}.html`）
> **Spring Boot 端口**：8081（`http://localhost:8081/api/{page}.html`）

---

## 九、参考文献要求

- 至少15篇，中英文兼具
- 近5年文献占大多数
- 重点参考类型：Spring Boot官方文档、Vue.js官方文档、MyBatis Plus官方文档、JWT安全机制相关论文、流浪宠物救助领域研究论文

**格式示例**：
```
[1] 作者姓名. 文章标题[J]. 期刊名, 年, 卷(期): 起讫页.
[2] 作者姓名. 书名[M]. 版次. 出版地: 出版者, 年.
```

---

## 十、文档更新记录

| 日期 | 版本 | 更新内容 |
|------|------|----------|
| 2026-03-31 | 创建 | 初始版本 |
| 2026-04-12 | v1.2.0 | 新增志愿者成长体系、宠物收藏、JWT双Token素材 |
| 2026-04-20 | v1.2.3 | 更新到16个页面、补充多申请人对比/回访记录、补充答辩Q9~Q10 |
| 2026-04-27 | v1.2.4 | 新增个性化推荐算法代码片段、创新点扩充、答辩Q10-Q12更新、论文章节调整 |

> 详细截图采集清单见 `截图管理文档.md`（共44张）
