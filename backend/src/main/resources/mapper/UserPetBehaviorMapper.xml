<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pet.rescue.mapper.UserPetBehaviorMapper">

    <!--
        插入行为记录，幂等写入。
        联合唯一键 (user_id, pet_id, behavior_type) 保证同一用户对同一宠物的同一行为不重复记录。
        ON DUPLICATE KEY UPDATE 仅更新时间戳，保证幂等性。
    -->
    <insert id="insertOrIgnore">
        INSERT INTO user_pet_behavior (user_id, pet_id, behavior_type, created_at)
        VALUES (#{userId}, #{petId}, #{behaviorType}, NOW())
        ON DUPLICATE KEY UPDATE created_at = NOW()
    </insert>

</mapper>
